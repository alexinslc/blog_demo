<div id="<%= dom_id post %>">
  <p>
    <h3><%= post.title %></h3>
    <h5>Posted By: <%= link_to post.user.name, user_path(post.user) %></h5>
    <h6><%= pluralize(post.views, "view") %></h6>
  </p>

  <p>
    <%= post.body %>
  </p>
</div>
